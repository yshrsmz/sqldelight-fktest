CREATE TABLE author (
    id INTEGER NOT NULL PRIMARY KEY,
    name TEXT NOT NULL,
    birthday TEXT NOT NULL
);

insert:
INSERT OR REPLACE INTO author (id, name, birthday) VALUES (?, ?, ?);

deleteAll:
DELETE FROM author;

selectAll:
SELECT * FROM author;

count:
SELECT COUNT(*) FROM author;
